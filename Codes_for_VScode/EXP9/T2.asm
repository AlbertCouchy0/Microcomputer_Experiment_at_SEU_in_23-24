DATA SEGMENT;
    NUM1 DW 2945
    NUM2 DW 6371
DATA ENDS

CODE SEGMENT;
    ASSUME CS:CODE, DS:DATA ;
MAIN PROC FAR ;
START: 
    PUSH DS
    SUB AX, AX
    PUSH AX
    MOV AX, DATA
    MOV DS, AX
BEGIN: 
    MOV AX, NUM1
    MOV BX, NUM2
    XOR AX, BX ;
    TEST AX, 0001 ;
    JZ CLASS;
    TEST BX, 0001
    JZ EXIT
    XCHG BX, NUM1 ;
    MOV NUM2, BX
    JMP EXIT;
CLASS: 
    TEST BX, 0001
    JZ EXIT
    INC NUM2
    INC NUM1 
EXIT: RET ;

MAIN ENDP ;
CODE ENDS
END START ;
