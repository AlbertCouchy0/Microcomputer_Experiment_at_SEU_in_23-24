DATA SEGMENT
STR DB 'Hello world!',0DH,0AH,'$' 
DATA ENDS

SSEG SEGMENT PARA STACK 'STACK'
DB 50 DUP(0)
SSEG ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:SSEG 
START:
        MOV AX,DATA
        MOV DS,AX
        MOV AX,SSEG
        MOV SS,AX
        MOV DX,OFFSET STR
        MOV AH,9
        INT 21H
        MOV AH,4CH
        INT 21H
CODE ENDS

END START
